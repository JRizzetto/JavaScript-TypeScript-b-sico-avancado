

const pontuacaoUsuario = 1001

const classificacaoSuruario = pontuacaoUsuario >= 1000 ? "Usuáriao VIP" : "Usuário normal";
console.log(classificacaoSuruario)

// Fallback

const usuarioCor = full;
const corPadrao = usuarioCor || "Verde";
console.log(corPadrao)